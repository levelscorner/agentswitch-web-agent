# Runtime deps for the agent + harness.
# The MCP client (agent/client.py) is otherwise stdlib-only; certifi just gives it
# a CA bundle so python.org Python on macOS can verify the platform's TLS cert.
anthropic>=1.7,<2
certifi>=2024.0
