# Copy to .env and fill locally. .env is git-ignored — NEVER commit real values.

# Which business to hit. Suryodaya (India) first; Keystone (US) for the contrast.
AS_BASE=https://agentswitch.theschoolofai.in
# AS_BASE=https://class.agentswitch.theschoolofai.in

# Team login. Password is per-business and comes from our team channel.
AS_EMAIL=team09@theschoolofai.in
AS_PASSWORD=

# Our own agent's model key (the agent runs on our machine with our keys).
# Fill whichever provider we use; leave the rest blank.
GEMINI_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
